import { Card, CardHeader } from "@/components/ui/card";
import React from "react";

const Grades = () => {
    return (
        <Card className="flex flex-col col-span-2">
            <CardHeader>Grades</CardHeader>
        </Card>
    );
};

export default Grades;
